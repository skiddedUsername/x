// updateenv.js — disabled for Railway deployment
// Original functionality wrote to a .env file which Railway does not support.
// Keeping a no-op export so imports don't break.

module.exports = function updateEnv() {
  // No operation
  return;
};
